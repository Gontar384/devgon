{
  "name": "devgon",
  "version": "1.0.0",
  "description": "devgon's modular and innovative web project",
  "author": "devgon<devgonteam@gmail.com> - Gontar",
  "private": true,
  "license": "UNLICENSED",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:test": "concurrently \"npm run dev:test --prefix backend\" \"npm run dev --prefix frontend\"",
    "test:all": "echo '▶️ BACKEND TESTS' && npm run test:all --prefix backend && echo '▶️ FRONTEND TESTS' && npm run test:all --prefix frontend",
    "test": "start-server-and-test dev:test http://localhost:3000 \"test:all\"",
    "start": "concurrently \"npm run start --prefix backend\" \"npm run start --prefix frontend\"",
    "build": "echo '📦 Building BACKEND...' && npm run build --prefix backend && echo '🌐 Building FRONTEND...' && npm run build --prefix frontend",
    "lint": "npm run lint --prefix backend && npm run lint --prefix frontend",
    "lint:fix": "npm run lint:fix --prefix backend && npm run lint:fix --prefix frontend",
    "format": "npm run format --prefix backend && npm run format --prefix frontend",
    "format:fix": "npm run format:fix --prefix backend && npm run format:fix --prefix frontend",
    "check:code-style": "echo '🧹 LINTING BACKEND' && npm run lint --prefix backend && echo '🧹 LINTING FRONTEND' && npm run lint --prefix frontend && echo '🎨 FORMAT CHECK BACKEND' && npm run format --prefix backend && echo '🎨 FORMAT CHECK FRONTEND' && npm run format --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.2.0",
    "start-server-and-test": "^2.0.12"
  }
}
