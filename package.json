{
  "name": "devgon",
  "version": "1.0.0",
  "description": "devgon's modular and innovative web project",
  "author": "devgon<devgonteam@gmail.com> - Gontar",
  "private": true,
  "license": "UNLICENSED",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:ci": "concurrently \"npm run dev:ci --prefix backend\" \"npm run dev --prefix frontend\"",
    "test:ci": "echo '▶️ BACKEND TESTS' && npm run test:ci --prefix backend && echo '▶️ FRONTEND TESTS' && npm run test:ci --prefix frontend",
    "ci": "start-server-and-test dev:ci http://localhost:3000 \"test:ci\"",
    "start": "concurrently \"npm run start --prefix backend\" \"npm run start --prefix frontend\"",
    "build": "npm run build --prefix backend && npm run build --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.2.0",
    "start-server-and-test": "^2.0.12"
  }
}
